<tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= user.team&.name || "No team" %></td>
    <td><%= user.role&.name || "No role" %></td>
    <td><%= user.created_at.strftime("%Y-%m-%d") %></td>
    <td>
        <div class="td-action">
            <%= link_to [:admin, user], class: "btn btn-sm btn-info flex-fill text-while" do %>
                <i class="fa fa-eye"></i>
            <% end %>
            <%= link_to edit_admin_user_path(user), class: "btn btn-sm btn-primary flex-fill text-while" do %>
                <i class="fas fa-edit"></i>
            <% end %>
            <%= button_to [:admin, user],
                method: :delete,
                data: { turbo_confirm: "Bạn có chắc chắn muốn xóa user này không?" },
                class: "btn btn-sm btn-danger flex-fill text-while" do %>
                <i class="fas fa-trash"></i>
            <% end %>
        </div>
    </td>
</tr>